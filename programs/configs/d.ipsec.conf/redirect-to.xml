  <varlistentry>
  <term><emphasis remap='B'>redirect-to</emphasis></term>
  <listitem>
<para>
Specify what address to put in IKEv2 REDIRECT notify payloads
(where to redirect the other end). Both IPv4 and IPv6 addresses
are supported as well the FQDNs. Only one address should be
specified.</para>
<para>
The value of this option is not considered at all if
send-redirect is set to no.
</para>
  </listitem>
  </varlistentry>
